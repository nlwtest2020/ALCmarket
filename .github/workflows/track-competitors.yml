name: Track Competitors - Every 24hrs

on:
  schedule:
    - cron: '0 2 * * *'  # 2 AM UTC daily
  workflow_dispatch:      # Manual trigger

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: claude/build-competitor-tracker-0U6x1

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -q pandas requests beautifulsoup4

      - name: Run competitor scraper
        run: python3 run_all.py

      - name: Run enhanced intelligence scraper
        run: python3 scrape_intelligence.py

      - name: Build live data JSON for dashboard
        run: python3 build_dashboard_data.py

      - name: Commit and push updates
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add -A data/ docs/data/
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update competitor tracking data $(date -u +'%Y-%m-%d %H:%M:%S UTC')" && git push origin claude/build-competitor-tracker-0U6x1)

      - name: Deploy to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs'

      - name: Publish to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
